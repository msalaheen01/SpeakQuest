{
  "name": "speakquest",
  "version": "1.0.0",
  "description": "SpeakQuest - Speech Therapy App Prototype",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": ["speech-therapy", "nextjs", "express"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}

